import styled from "styled-components";
import BadgeActivity from "./BadgeActivity";

export const BadgeActivityStyle = styled(BadgeActivity)`
  background-color: ${(props) => props.backgroundColor};
`;
